services:
  semanna8-app-2:
    build: .
    ports:
      - "8082:8080"
    volumes:
      - ./src/main/resources/oracle/wallet:/app/wallet:ro
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    restart: unless-stopped